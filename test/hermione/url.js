const URL = "http://localhost";
const PORT = 3000;
const BASE_URL = `${URL}:${PORT}`;
const MAIN_URL = `${BASE_URL}/hw/store`;
const CATALOG_URL = `${BASE_URL}/hw/store/catalog`;

module.exports = {
  BASE_URL,
  MAIN_URL,
  CATALOG_URL,
};
